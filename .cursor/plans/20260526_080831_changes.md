---
saved_at: "2026-05-26T06:08:31.695Z"
---

Updated the PR → Linear sync hook so a **dedicated `save_comment`** with the PR link is required, not only issue links/status via `save_issue`.

### Changes

**`.cursor/hooks/pr-linear-sync.cjs`**
- Step 4 in injected agent instructions: call `save_comment` on the confirmed issue with a fixed markdown body (PR link + test-plan checklist).
- `buildPrCommentBody()` supplies that template.
- `beforeMCPExecution` also gates `save_comment` (approval prompt before posting).
- Pending state tracks `issueSynced` and `commentSynced`; it clears only after **both** `save_issue` and `save_comment` succeed.

**`.cursor/hooks.json`**
- Registered `save_comment` on `beforeMCPExecution` and `afterMCPExecution`.

### Flow after `gh pr create`

1. Hook injects instructions (find issue → user confirms).
2. `save_issue` — PR link on issue + **In Review** (approval prompt).
3. `save_comment` — comment body includes **GitHub PR:** `<url>` (approval prompt).
4. Pending sync clears when both calls succeed.

Reload hooks in Cursor (or restart) so `hooks.json` changes apply.

The comment template looks like:

```markdown
**GitHub PR:** https://github.com/ZawilecxD/trAInR/pull/42

## Test plan
- [ ] CI passes (lint + build)
- [ ] Manual smoke test on preview deploy
```